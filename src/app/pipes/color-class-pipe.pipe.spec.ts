import { ColorClassPipePipe } from './color-class-pipe.pipe';

describe('ColorClassPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorClassPipePipe();
    expect(pipe).toBeTruthy();
  });
});
